<!DOCTYPE html>
<html lang="en">
  {{> head }}
  <body>
    <div class="container-fluid">
      <div class="row nav-row">
        <div class="col-md-12">
          {{> nav }}
        </div>
      </div>
    </div>
    <div class="container content">
      <div class="row">
        <div class="col-md-offset-1 col-md-10">
          <h1 class="text-center"> {{#if pageTitle}}{{pageTitle}}{{else}}{{title}}{{/if}} </h1>
        </div>
      </div>
      {{> posts }}
    </div>

    {{> footer }}
    {{> jslibs }}
  </body>
</html>
